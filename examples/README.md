# SheetFlow v0.2.0 示例

- `sample_input`：可公开使用的虚构订单数据。
- `workflows/basic.yaml`：合并、清理与去重。
- `workflows/split.yaml`：按列值拆分文件。
- `workflows/summary.yaml`：分组汇总。

v0.2.0 还可以通过 GUI 的中文描述或 CLI `suggest` 命令，基于实际列名生成可编辑工作流。
