# 贡献指南

1. 从 `main` 创建短期分支。
2. 不提交真实业务数据、密钥、缓存或构建产物。
3. 变更核心行为时补充测试，保证 `pytest`、`ruff check .` 和 `mypy src` 通过。
4. 一个提交只处理一类问题，并在说明中写清行为变化与验证结果。
5. 新增依赖前检查许可证，并说明为什么现有依赖不能满足需求。

提交贡献即表示你同意按 Apache-2.0 许可证发布相关代码。

